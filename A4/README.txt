#Project built with cmake#

Loads various .obj files (teapot, bunny) in a freelook environment that can be maneuvered.
The objects scale and sheer over time and have been given random colors/positions

User inputs:

Move cursor to look around (works best in fullscreen)

w: move forward
a: move left
s: move back
d: move right
